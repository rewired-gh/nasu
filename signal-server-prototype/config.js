const config = {
  maxSessionLength: 128,
  maxStringLength: 0x400,
  portHttp: 9753,
  allowedOrigins: [
    "https://nasu.hopp.top",
    "https://nasu.netlify.app",
    // "http://localhost:5173",
  ],
};

export default config;
