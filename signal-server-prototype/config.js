const config = {
  maxSessionLength: 128,
  maxStringLength: 0x400,
  portHttp: 9753,
  allowedOrigins: [
    "https://nasu.hopp.top",
    "https://nasu.netlify.app",
    "https://dm.hopp.top",
  ],
};

export default config;
